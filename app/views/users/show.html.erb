<div class="main-wrapper">
  <div class="container">
    <div class="search-products-tab"><%= "「#{@user.name}」のお気に入りリスト" %></div>
    <div class="row">
      <% @favorites_products.each do |product| %>
      <%= render "shared/product_segment", product: product %>
      <% end %>
    </div>
  </div>
</div>
